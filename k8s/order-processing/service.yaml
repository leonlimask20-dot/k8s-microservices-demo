apiVersion: v1
kind: Service
metadata:
  name: order-processing-service
  namespace: microservices
spec:
  selector:
    app: order-processing
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
