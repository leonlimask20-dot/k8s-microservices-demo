apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: microservices
spec:
  selector:
    app: kafka
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
  clusterIP: None  # Headless para StatefulSet
